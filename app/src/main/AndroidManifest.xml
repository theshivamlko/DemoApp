<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.shivam.navoki.demoapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".SingletonClass"
        android:allowBackup="true"
        android:icon="@drawable/ic_audiotrack_black_24dp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainPage" />
        <activity android:name=".Layouts" />
        <activity android:name=".ViewsExample1" />
        <activity android:name=".ViewExample2" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ListViewActivity2" />
        <activity android:name=".IntentActivity2">

            <!--
             <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                 <data android:mimeType="text/plain"></data>
             </intent-filter>
            -->

        </activity>
        <activity android:name=".IntentActivity3" />
        <activity android:name=".IntentActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".NewActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".Main3ActivityCycle" />
        <activity android:name=".DrawableActivity"></activity>
        <activity android:name=".TemporaryStorage">

        </activity>

        <receiver android:name=".Recievers">
            <intent-filter>
                <action android:name="android.intent.action.REBOOT" />
            </intent-filter>
        </receiver>

        <activity android:name=".Dialogs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>